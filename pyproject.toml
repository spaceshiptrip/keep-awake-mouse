[project]
name = "keep-awake-mouse"
version = "0.1.1"
description = "Tiny Python app that jiggles the mouse to keep the computer awake."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
mouse-mover = "mouse_mover:main"
